import styled from '@emotion/native';

export const PlayerTilesLeftWrapper = styled.View({
  flexDirection: 'row',
  flexWrap: 'wrap',
  justifyContent: 'flex-start',
  paddingHorizontal: 7.5,
  paddingBottom: 7.5,
});
