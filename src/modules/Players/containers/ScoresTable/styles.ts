import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'relative',
    flexGrow: 1,
    flexDirection: 'row',
  },
  listContent: {
    position: 'relative',
    flexGrow: 1,
    flexDirection: 'row',
  },
});
