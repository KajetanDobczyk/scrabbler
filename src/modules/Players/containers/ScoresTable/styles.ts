import { StyleSheet } from 'react-native';

import { color } from 'src/theme';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    flexGrow: 1,
  },
  playersMoves: {
    flexGrow: 1,
    flexDirection: 'row',
  },
});
